{
  "name": "documentation",
  "version": "1.0.0",
  "description": "The Dirt-Simple PostGIS HTTP API serves Swagger documentation from here using `fastify-static` rather than using the default `fastify-swagger` documentation path, relying on `fastify-swagger` only to create the Swagger JSON. This yields several advantages:",
  "main": "swagger-ui-bundle.js",
  "scripts": {
    "start": "http-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "filepond": "^4.4.0"
  }
}
